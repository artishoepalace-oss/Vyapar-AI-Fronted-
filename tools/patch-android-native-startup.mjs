import fs from 'node:fs';

const path = 'android-app/app/src/main/java/com/vyaparai/app/MainActivity.java';
let text = fs.readFileSync(path, 'utf8');

const oldFinished = `                view.evaluateJavascript(\n                        "!!(window.__vyaparStartupCoordinator && document.documentElement.getAttribute('data-vyapar-session'))",\n                        value -> { if (!"true".equals(value)) dismissStartupCover(); });\n                view.postOnAnimation(() -> {\n                    view.evaluateJavascript("window.onNativeAppReady && window.onNativeAppReady();", null);\n                });`;

const newFinished = `                view.evaluateJavascript(\n                        "(function(){var r=document.documentElement,s=r&&r.getAttribute('data-vyapar-session');return !!(window.__vyaparStartupCoordinator&&s&&s!=='restoring');})()",\n                        value -> {\n                            if (!"true".equals(value)) {\n                                view.evaluateJavascript("(function(){var d=document,r=d.documentElement,g=d.getElementById('vy855BootGuard');if(g)g.remove();if(r)r.classList.remove('vy855-booting');})();", null);\n                                dismissStartupCover();\n                            }\n                        });\n                view.postOnAnimation(() -> {\n                    view.evaluateJavascript("window.onNativeAppReady && window.onNativeAppReady();", null);\n                });\n                // Absolute native deadline: even if a later bundled module throws,\n                // the user must never be left behind an opaque startup surface.\n                view.postDelayed(() -> {\n                    if (startupCover == null) return;\n                    view.evaluateJavascript(\n                            "(function(){var d=document,r=d.documentElement,g=d.getElementById('vy855BootGuard'),s=r&&r.getAttribute('data-vyapar-session'),st=window.VyaparStorage,ready=(s==='login')||(s&&s!=='restoring'&&st&&st.bootReady===true);if(g)g.remove();if(r)r.classList.remove('vy855-booting');if(!ready&&d.body&&!d.getElementById('vyNativeStartupRecovery')){var e=d.createElement('div');e.id='vyNativeStartupRecovery';e.style.cssText='position:fixed;inset:0;z-index:2147483647;display:flex;align-items:center;justify-content:center;padding:24px;background:#000;color:#fff;font-family:system-ui,-apple-system,sans-serif;text-align:center';e.innerHTML='<div style=\\\"width:min(360px,100%);padding:24px;border-radius:22px;background:#171717\\\"><strong style=\\\"display:block;font-size:20px;margin-bottom:10px\\\">Vyapar AI could not finish opening</strong><p style=\\\"margin:0 0 18px;line-height:1.45;color:#cfcfcf\\\">Your saved business data has not been cleared or overwritten.</p><button type=\\\"button\\\" style=\\\"border:0;border-radius:14px;padding:12px 18px;font-weight:700\\\">Reopen app</button></div>';var b=e.getElementsByTagName('button')[0];if(b)b.onclick=function(){location.reload();};d.body.appendChild(e);}})();",\n                            null);\n                    dismissStartupCover();\n                }, 10000L);`;

if (!text.includes(oldFinished)) throw new Error('MainActivity onPageFinished startup block changed; patch not applied.');
text = text.replace(oldFinished, newFinished);

const oldDismiss = `    private void dismissStartupCover() {\n        if (startupCover == null || startupCoverDismissQueued || webView == null) return;\n        startupCoverDismissQueued = true;\n        // API 23+, matching minSdk: release only when the DOM is ready to draw.\n        webView.postVisualStateCallback(1L, new WebView.VisualStateCallback() {\n            @Override\n            public void onComplete(long requestId) {\n                if (startupCover == null) return;\n                final FrameLayout cover = startupCover;\n                cover.animate().alpha(0f).setDuration(160L).withEndAction(() -> {\n                    ViewGroup parent = (ViewGroup) cover.getParent();\n                    if (parent != null) parent.removeView(cover);\n                    if (startupCover == cover) startupCover = null;\n                }).start();\n            }\n        });\n    }`;

const newDismiss = `    private void dismissStartupCover() {\n        if (startupCover == null || startupCoverDismissQueued) return;\n        startupCoverDismissQueued = true;\n        final FrameLayout cover = startupCover;\n        cover.post(() -> {\n            ViewGroup parent = (ViewGroup) cover.getParent();\n            if (parent != null) parent.removeView(cover);\n            if (startupCover == cover) startupCover = null;\n        });\n    }`;

if (!text.includes(oldDismiss)) throw new Error('MainActivity dismissStartupCover block changed; patch not applied.');
text = text.replace(oldDismiss, newDismiss);
fs.writeFileSync(path, text, 'utf8');
console.log('Android native startup hard deadline applied.');
